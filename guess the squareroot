# Using the pow() function in the program to guess the square root1
//dot net uses 'using' instead of importa1
//dot net uses 'using' instead of importa1
//dot net uses 'using' instead of import
#The program is to have a quiz contest.
#Code begins here
# Code starts here
import math
num = float(input(" Enter a number: "))
sqRoot = math.pow(num, 0.5)
print("The square root of a given number {0} = {1}".format(num, sqRoot)) 
# End of program
//dot net uses 'using' instead of import
